# دليل التحكم بمواقع الدروس على الخريطة

دليل سريع للأدمن — كيف تنسّق مواقع الـ Units على صفحة `/map` من غير ما تلمس
الكود نهائياً.

---

## ١. الفكرة باختصار

كل وحدة (Unit) في قاعدة البيانات بصير عندها أربعة حقول إضافية للخريطة:

| الحقل             | الوظيفة                                                         | المدى     |
|-------------------|-----------------------------------------------------------------|-----------|
| `map_x`           | الموقع الأفقي للـ pin بالنسبة المئوية من عرض الخريطة            | 0 – 100   |
| `map_y`           | الموقع العمودي للـ pin بالنسبة المئوية من ارتفاع الخريطة        | 0 – 100   |
| `map_size`        | حجم الـ pin (Small / Medium / Large) — اختياري                  | نص قصير   |
| `map_image_path`  | صورة بديلة للـ pin (لو بدك pin مختلف عن صورة الـ unit الأصلية) | مسار صورة |

> 💡 الاتفاقية: `map_x = 0` يعني أقصى يسار، `map_x = 100` يعني أقصى يمين.
> `map_y = 0` يعني فوق، `map_y = 100` يعني تحت.

---

## ٢. أسهل طريقة (بدون كود) — لوحة الأدمن

١. سجّل دخول كحساب Admin.
٢. روح على `/admin/units`.
٣. كل صف فيه قسم **"Map placement"** فيه ثلاث طرق للتحكم:

### أ. سحب الـ pin بالماوس
- فيه معاينة مصغّرة للخريطة بنفس الخلفية اللي بيشوفها الطفل.
- اضغط على الـ pin واسحبه لأي مكان بدك إياه.
- الموقع بيتحدّث على طول وبيُحفظ بشكل تلقائي لما تترك الماوس.

### ب. أزرار الاتجاهات (◀ ▲ ▼ ▶)
- مفيدة لما تبغى ضبط دقيق بدل السحب.
- اختر حجم الخطوة (1% أو 2% أو 5%) من الأزرار اللي تحت الـ keypad.
- كل ضغطة على سهم بتحرّك الـ pin بمقدار الخطوة المختارة.
- زر `✓` الأخضر بيحفظ الموقع فوراً.

### ج. مواقع جاهزة (Quick spots)
- ٩ أزرار صغيرة (الزوايا التسع: شمال يسار، شمال وسط، شمال يمين، إلخ).
- ضغطة وحدة بتنقل الـ pin لموقع شائع. مفيدة لما تبدأ من الصفر.

### د. الأرقام الدقيقة
- لو بدك تحط رقم محدد مثل `35.5%`، اكتبه في الحقل اللي تحت المعاينة.
- بيُحفظ تلقائي لما تنقر برّا الحقل.

---

## ٣. تغيير حجم الـ pin

من نفس صف الـ unit في `/admin/units`، فيه قائمة منسدلة `Map size`:

- **Small** — مناسب للـ units اللي صورها بسيطة (book bag، toy).
- **Medium** — الحجم الافتراضي. مناسب لمعظم الـ units.
- **Large** — للـ units المهمة اللي بدك تخليها أبرز.

---

## ٤. صورة Pin مختلفة

أحياناً صورة الـ unit الأصلية ما بتطلع حلوة على الخريطة (مثلاً صورة معقدة فيها
تفاصيل صغيرة). بقدر تحط صورة pin بديلة مخصوصة:

١. ارفع الصورة إلى `public/assets/lessons/your-unit/pin.png` (أو أي مسار).
٢. في صف الـ unit بالأدمن، حط المسار في حقل **"Map icon override"** (مثلاً
   `assets/lessons/family/pin.png`).
٣. خليه فاضي إذا بدك تستخدم صورة الـ unit الأصلية.

---

## ٥. ترتيب الـ Arena

البطلة الـ Games Arena ما إلها صف منفصل في الأدمن، لكن بتتموضع تلقائياً
بناءً على موقع Unit 2 (Family). إذا حركت Unit 2، الـ Arena بيتحرك معها
بنفس النسبة.

التفاصيل: في `MapController::index()` فيه السطور:

```php
$u2 = $units->firstWhere('number', 2);
$arenaX = $u2 && $u2['map_x'] !== null ? max(8, (float) $u2['map_x'] - 18) : 38;
$arenaY = $u2 && $u2['map_y'] !== null ? (float) $u2['map_y'] : 50;
```

> 💡 لو بدك تموضع الـ Arena بشكل مستقل عن Unit 2، استبدل هذه السطور بـ
> قراءة من إعدادات صريحة (مثلاً `config('kiddo.arena.map_x')`).

---

## ٦. خطوات منطقية لو بدك تنسيق جديد كامل

١. افتح `/map` في تبويب جنبي.
٢. افتح `/admin/units` في التبويب الرئيسي.
٣. لكل unit، اسحب الـ pin بالـ MapPositionEditor.
٤. ارجع للـ `/map` واعمل refresh — بتشوف الـ pin بمكانه الجديد.
٥. كرّر للوحدات كلها حتى تحصل على التنسيق اللي بدك إياه.

---

## ٧. مثال لقيم جاهزة (التنسيق الحالي)

من `add_map_position_to_units` migration:

| Unit | code | map_x | map_y | الموقع تقريباً       |
|------|------|-------|-------|----------------------|
| U0   | U0   | 20.5  | 37    | يسار، فوق المنتصف     |
| U1   | U1   | 72.0  | 33    | يمين، فوق المنتصف     |
| U2   | U2   | 72.0  | 65    | يمين، تحت المنتصف     |

تقدر تستخدم هذه كنقطة بداية وتعدّل من هناك.

---

## ٨. Troubleshooting سريع

| المشكلة                                         | الحل                                                                  |
|-------------------------------------------------|-----------------------------------------------------------------------|
| الـ pin مش طالع نهائياً                         | تأكد إن `map_x` و `map_y` محفوظين (مش فاضيين). Refresh `/map`.        |
| الـ pin طالع ببطء أو بمكان غريب                 | امسح الـ browser cache (Ctrl+Shift+R) وفتش `/api/audio/check`.        |
| الـ pin طالع لكن صورته مش ظاهرة                 | تأكد إن `image_path` أو `map_image_path` بيشير لملف موجود فعلاً.      |
| التغييرات في الأدمن مش بتنحفظ                  | شوف console المتصفح — لو فيه 422، إعد فحص قيم الـ x/y أنها بين 0–100. |
| كل pin طالع بنفس الموقع                         | الـ migration الافتراضي بيحط القيم لـ U0/U1/U2 بس. وحدات جديدة لازم تحط لها قيم يدوياً. |

---

## ٩. تحت الكاپوت (للمطورين)

- البيانات بتيجي من `MapController::index()` كـ Inertia props.
- صفحة `MapScreen.jsx` فيها fallback dictionaries (`UNIT_VISUAL`,
  `ARENA_VISUAL`) لكن قيم الـ DB دائماً تتقدم على الـ fallback عبر دالة
  `visualFor(unit)`.
- لو شي وحدة ما إلها `map_x`/`map_y`، بتستخدم الـ default من dictionaries.
- بعد التعديل في الأدمن، PATCH بيروح لـ `/admin/units/{id}` بـ JSON
  `{ map_x, map_y, map_size, map_image_path }` — فحص validation:
  - `map_x | map_y`: `nullable|numeric|min:0|max:100`
  - `map_size`: `nullable|string|max:128`
  - `map_image_path`: `nullable|string|max:255`

---

> ✨ **الخلاصة:** كل تنسيق للـ map ممكن يصير من لوحة الأدمن. ما في حاجة
> لتعديل الكود نهائياً.
